{% extends 'BoshCoreBundle:Deployment:layout.html.twig' %}

{% block def_title 'Deployments' %}

{% block def_description %}
  An encapsulation of software and configuration that BOSH can deploy to the cloud. You can think of a deployment as the state of a collection of VMs: what software is on them, what resources they use, and how these are orchestrated.
{% endblock %}

{% block frag_content %}
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Deployment</th>
      </tr>
    </thead>
    <tbody>
      {% for result in data.results %}
        <tr>
          <td><a href="{{ path('bosh_core_deployment_index', { 'deployment' : result.name, '_format' : 'html' }) }}">{{ result.name }}</a></td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
{% endblock %}

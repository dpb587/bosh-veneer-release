{% if plan.action == 'unchanged' %}
    <span class="text-muted">The director's cloud config appears to match.</span>
{% else %}
    <pre><code>{% for line in plan.diff %}{{ line }}
{% endfor %}</code></pre>
{% endif %}
